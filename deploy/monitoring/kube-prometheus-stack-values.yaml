global:
  rbac:
    create: true
  imagePullSecrets:
    - name: acr-pull-secret

grafana:
  adminPassword: changeme
  replicaCount: 1
  grafana.ini:
    server:
      root_url: "%(protocol)s://20.1.152.210/grafana/"
      serve_from_sub_path: true
  image:
    registry: microservicesdev5912t.azurecr.io
    repository: monitoring/grafana
    tag: 12.2.0
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi
  tolerations:
    - key: CriticalAddonsOnly
      operator: Exists
  service:
    type: ClusterIP
    annotations: {}
  ingress:
    enabled: false
  persistence:
    enabled: false
  sidecar:
    dashboards:
      enabled: false
    datasources:
      enabled: false

prometheus:
  enabled: true
  service:
    type: ClusterIP
    annotations: {}
  prometheusSpec:
    externalUrl: http://20.1.152.210/prometheus/
    routePrefix: /prometheus
    retention: 1d
    scrapeInterval: 30s
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 300m
        memory: 512Mi
    serviceMonitorSelectorNilUsesHelmValues: false
    tolerations:
      - key: CriticalAddonsOnly
        operator: Exists

prometheus-node-exporter:
  enabled: false

kubeStateMetrics:
  enabled: false

prometheusOperator:
  image:
    registry: microservicesdev5912t.azurecr.io
    repository: monitoring/prometheus-operator
    tag: v0.86.1
  tolerations:
    - key: CriticalAddonsOnly
      operator: Exists
  admissionWebhooks:
    enabled: false
    patch:
      enabled: false
  tls:
    enabled: false

alertmanager:
  enabled: false
